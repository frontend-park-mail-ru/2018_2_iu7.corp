extends base.pug

block content
    div
        table 
            thead
                tr
                    th(class="leaderboard__header-left") Имя пользователя
                    th(class="leaderboard__header-right") Счёт
            tbody
                each user in usrs
                    tr
                        td= user.username
                        td= user.score
    div
        p
            a(href="#" id="prev_page_link" class="nav_link") Туда
            a(href="#" id="next_page_link" class="nav_link") Сюда
            